// Generated by Version.cmake -- do not edit.
//
// C++20/23 version constants. Configure via CMake variables:
//
//   VERSION_H_FILENAME  -- set to "version.hpp" (or "${VERSION_PREFIX}Version.hpp")
//                          to select this template over the default Version.h.in.
//   VERSION_NAMESPACE   -- optional C++ namespace, e.g. "myapp::version".
//                          If empty, constants are emitted at global scope.
//   VERSION_PREFIX      -- optional prefix for the C preprocessor macros below,
//                          e.g. "MYAPP_" produces MYAPP_VERSION_MAJOR etc.
//
// The C preprocessor macros at the bottom of this file ensure the header
// remains usable from C and from C++ code that includes it before C++20.
#pragma once

#include <cstdint>
#include <string_view>

@_VERSION_NAMESPACE_BEGIN@

/// Major version component (breaking changes).
inline constexpr std::uint32_t  version_major   = @_VERSION_MAJOR@;
/// Minor version component (backward-compatible additions).
inline constexpr std::uint32_t  version_minor   = @_VERSION_MINOR@;
/// Patch version component (backward-compatible fixes).
inline constexpr std::uint32_t  version_patch   = @_VERSION_PATCH@;
/// Number of commits since the last version tag (0 on a clean tagged release).
inline constexpr std::uint32_t  version_commit  = @_VERSION_COMMIT@;
/// Short git hash of the current commit.
inline constexpr std::string_view version_sha    = "@_VERSION_SHA@";
/// Semantic version string: "major.minor.patch.commit".
inline constexpr std::string_view version_string = "@_VERSION_SEMANTIC@";
/// Full git-describe output, including dirty flag if applicable.
inline constexpr std::string_view version_full   = "@_VERSION_FULL@";

@_VERSION_NAMESPACE_END@

// ── C preprocessor macros ────────────────────────────────────────────────────
// Retained for C ABI headers and pre-C++20 code. The VERSION_PREFIX variable
// scopes these to avoid collisions when multiple libraries are present.
#define @_VERSION_PREFIX@VERSION_MAJOR   @_VERSION_MAJOR@
#define @_VERSION_PREFIX@VERSION_MINOR   @_VERSION_MINOR@
#define @_VERSION_PREFIX@VERSION_PATCH   @_VERSION_PATCH@
#define @_VERSION_PREFIX@VERSION_COMMIT  @_VERSION_COMMIT@
#define @_VERSION_PREFIX@VERSION_SHA     "@_VERSION_SHA@"
#define @_VERSION_PREFIX@VERSION_STRING  "@_VERSION_SEMANTIC@"
#define @_VERSION_PREFIX@VERSION_FULL    "@_VERSION_FULL@"
